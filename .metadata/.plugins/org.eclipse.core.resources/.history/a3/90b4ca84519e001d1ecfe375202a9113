/*var select = document.getElementById('DashHelpDesk')

select.addEventListener('change', function(){
	var url = window.location;
    var valor = select.value;
    var result = url +"?Parametros=" +  valor ;
    console.log(result)
	location.href = '?Parametros=' + valor;
})




// adiciona evento quando o usuário selecionar outro valor
select.change(function() {
  // pega valor do option selecionado
  var valor = meu_select.val()
  alert('Seu valor é: ' + valor);
  // muda url do navegador
  location.href = '#?param1=' + valor;
})*/


function selecionar(){

  var pacote = document.getElementsByClassName('.form-check-input');
    for (var i = 0; i < pacote.length; i++){
        if ( pacote[i].checked ) {
            if(pacote[i].value == "a"){
                var Pacote_i = "adm";
                alert(Pacote_i);
            }else if (pacote[i].value == "") {
                var Pacote_ii = "Pacote DOIS";
                alert(Pacote_ii);
            }else if (pacote[i].value == "Pacote iii") {
                var Pacote_iii = "Pacote TRÊS";
                alert(Pacote_iii);
            }
        }
    }
  
}